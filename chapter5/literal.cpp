#include <iostream>

// Literals are values that are inserted directly into the code.

int main() {
    bool isTrue {true}; // true is a boolean literal
    double d {1.2}; // 1.2 is a double literal
    std::cout << "isTrue: " << isTrue << std::endl; // "isTrue: 1" is a C-style string literal
}
